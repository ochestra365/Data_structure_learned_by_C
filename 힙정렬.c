/* 현재 요소의 개수가 heap_size인 힙 h에 item을 삽입한다*/
//최대 힙(max heap)삽입 함수
#include <stdio.h>
#include <stdlib.h>

struct 
typedef struct HeapType {

};
void insert_max_heap(HeapTypy* h, element item) {

}

int main() {

	return 0;
}